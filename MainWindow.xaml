<Window
    x:Class="lab8.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:lab8"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="Сортувати за алфавітом" Command="{Binding SortCommand}" Margin="0,0,0,10"/>

            <ListView Grid.Row="1" ItemsSource="{Binding Recipes}" BorderBrush="Gray" BorderThickness="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="18"/>
                            <TextBlock Text="{Binding Category.Name}" FontStyle="Italic" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Загальна кількість рецептів: " FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalCount}" FontWeight="Bold" Foreground="Blue"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Column="1" Spacing="10">
            <TextBlock Text="Додати новий рецепт" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <TextBlock Text="Назва:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding NewRecipeTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Інструкція:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding NewRecipeInstructions, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="100" TextWrapping="Wrap"/>

            <Button Content="Додати" Command="{Binding AddCommand}" HorizontalAlignment="Stretch" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>